/* Minimal stub so coreHTTP (a transitive dep of the Infineon MQTT library)
 * can compile. We don't use HTTP; all defaults from core_http_config_defaults.h
 * are fine as-is. */
#ifndef CORE_HTTP_CONFIG_H_
#define CORE_HTTP_CONFIG_H_
#endif /* CORE_HTTP_CONFIG_H_ */
